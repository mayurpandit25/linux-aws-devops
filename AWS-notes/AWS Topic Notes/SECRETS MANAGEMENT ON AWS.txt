**SECRETS MANAGEMENT ON AWS**

------------------------------------------------------------------------------------------------------------------------------------

1.system manager
if we uses the system manager and store the information it will be very easy to retrive our data means if a service access these data these service only having the iam access

-----------------------------------------------------------------

2.secret manager
generally used for rotating the certificates/data to reduces the risks of exposing our data

-----------------------------------------------------------------

3.hashicorp volt
hashicorp volt is a open source platform used for managing the secrets.

1.if ur using the multi cloud uses the hashicorp volt as well as before migrating on the aws cloud probably you can say we are using the hashicorp volt

-----------------------------------------------------------------
